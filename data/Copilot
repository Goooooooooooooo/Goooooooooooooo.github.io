# GitHub Copilot 全栈全局开发规则
## 生效范围
本规则为所有项目、所有语言、所有场景的兜底通用规则，对所有内联补全、Chat对话、代码重构、注释生成、测试用例生成100%生效；单项目可通过本地指令文件叠加补充规则，优先级高于全局规则。

---

## 【最高优先级·强制执行规则】先讨论、后编码
本条规则优先级高于本文件所有其他规则、任何用户单次提问的补充要求，Copilot必须100%严格执行，无任何例外。

### 1. 必须严格执行「先对齐、后编码」流程的场景
只要符合以下任意一种场景，禁止直接输出完整功能代码、大规模重构代码、技术方案实现代码，必须先完成需求对齐与方案确认：
- 新功能/新模块开发（包含单文件完整功能、跨文件业务逻辑）
- 需求描述不完整、边界条件不明确、技术选型未指定
- 代码重构涉及超过10行的逻辑修改、或影响原有业务逻辑
- 技术方案选型、架构设计、数据库表结构设计
- 测试用例全量生成、接口文档全量生成
- 任何用户明确要求「先讨论方案」的场景

### 2. 标准对齐流程（必须按顺序执行）
1.  需求复述：用简洁的语言复述用户的核心需求，确认理解无误
2.  待确认项梳理：列出影响代码实现的核心待确认点（技术栈、边界条件、约束要求、性能要求、兼容要求等）
3.  方案大纲输出：复杂需求必须输出分层的技术实现大纲，不包含具体代码，仅说明实现思路、模块拆分、核心逻辑
4.  等待用户确认：必须等待用户明确回复「确认方案」「可以开始编码」等确认指令后，才可输出对应代码

### 3. 可直接输出代码的例外场景
仅同时满足以下所有条件时，可直接输出代码，无需走确认流程：
- 当前打开文件上下文完整、技术栈明确、已有编码规范清晰
- 需求为单行/局部代码补全、已有函数的逻辑补充、单行注释生成、简单语法修复
- 代码修改不超过10行，不影响原有业务逻辑的核心流程
- 不涉及新功能、新模块、大规模重构

---

## 全栈通用编码准则（所有语言/所有项目强制遵守）
### 命名规范
- 变量/函数/方法：统一小驼峰 `camelCase`，布尔值必须以 `is/has/can/should` 开头
- 类/组件/枚举/类型/接口：统一大驼峰 `PascalCase`
- 常量：全大写下划线分隔 `UPPER_SNAKE_CASE`
- 文件名：统一小写中划线分隔 `kebab-case`，禁止中文、空格、特殊字符
- 命名必须语义化，禁止无意义缩写（行业通用缩写除外，如id、url、req、res、ctx）

### 格式与注释规范
- 缩进统一使用 2 空格，禁止Tab与空格混用
- 行尾无多余空格，文件末尾保留1个空行
- 注释统一使用中文，仅在关键逻辑、边界条件、复杂算法处添加单行注释
- 函数/类/接口必须添加对应语言的标准文档注释，包含入参、返回值、异常说明
- 禁止冗余注释、禁止注释掉的代码块、禁止与代码逻辑不符的无效注释

### 代码质量通用要求
- 代码必须简洁、可维护、符合对应语言的官方最佳实践
- 必须处理所有异常与边界情况，禁止吞异常、禁止无兜底的逻辑
- 禁止硬编码魔法值，必须使用常量替代
- 优先使用原生API，避免引入不必要的第三方依赖
- 重复逻辑必须封装为可复用的函数/组件/工具类
- 代码必须可测试，高内聚低耦合，职责单一

### 安全合规红线（所有场景强制遵守）
- 禁止硬编码密钥、密码、Token、AK/SK等敏感信息，必须使用环境变量替代
- 禁止生成有安全风险的代码，包括但不限于SQL注入、XSS漏洞、命令注入、权限绕过、路径遍历
- 禁止生成侵犯版权的代码，不直接复制未授权的开源代码
- 禁止生成违反法律法规的代码与内容

---

## 全栈分域通用规则
### 前端开发通用规则
- 严格遵循组件化开发，组件职责单一，禁止超大组件
- 样式与逻辑分离，禁止大量内联样式
- 必须处理响应式兼容、边界渲染异常、加载状态、空状态、错误状态
- 接口请求必须封装统一的请求拦截、响应拦截、错误处理
- 路由必须做权限控制、懒加载优化
- 禁止直接操作DOM，优先使用框架的响应式能力

### 后端开发通用规则
- 严格遵循分层架构，职责清晰，禁止跨层调用
- 入参必须做合法性校验，出参必须做格式统一封装
- 数据库操作必须防止注入风险，优先使用预编译/ORM
- 接口必须符合RESTful/对应框架的规范，包含统一的响应格式、错误码
- 必须添加完整的日志记录，禁止日志泄露敏感信息
- 耗时操作必须做异步处理、超时控制、熔断降级

### 数据库开发通用规则
- 表名、字段名统一小写下划线分隔，必须添加中文注释
- 必须设置主键，禁止无主键表；索引设计必须符合最左匹配原则
- 禁止使用SELECT *，必须指定查询字段
- 大表查询必须做分页、限流，禁止全表扫描
- 必须处理事务边界，避免长事务、大事务
- 禁止在数据库中存储敏感明文信息

### 脚本/工具开发通用规则
- 必须添加使用说明、入参说明、异常处理
- 必须兼容跨平台场景，禁止硬编码系统专属路径
- 执行过程必须有清晰的日志输出，便于排查问题
- 必须做入参校验，禁止无校验的用户输入直接执行

---

## 主流语言/技术栈专属补充模板（自动适配对应文件）
---
### JavaScript/TypeScript 通用规范（适用文件：*.js, *.jsx, *.ts, *.tsx）
- 严格遵循 ES6+ 规范，优先使用 const/let，禁止 var
- TypeScript 必须添加完整的类型注解，禁止使用 any/unknown 类型，禁止类型断言滥用
- 异步代码优先使用 async/await，禁止回调地狱
- 优先使用箭头函数处理this指向，禁止滥用bind
- 导入导出必须使用 ESModule 规范，禁止混用 CommonJS
- 禁止全局变量污染，禁止修改原生对象原型

---
### React 生态规范（适用文件：*.jsx, *.tsx）
- 组件使用函数式组件 + React Hooks，禁止类组件
- 组件Props必须使用interface定义完整类型，禁止隐式any
- 状态管理优先使用项目已集成的方案，不额外引入新依赖
- 列表渲染必须添加稳定的key，禁止使用index作为key
- 副作用useEffect必须明确完整的依赖数组，禁止遗漏依赖
- 自定义Hooks必须以use开头，封装可复用逻辑
- 优先使用React.memo/useMemo/useCallback做性能优化，禁止过度优化

---
### Vue 生态规范（适用文件：*.vue）
- 使用 Vue 3 + Composition API + <script setup> 写法，禁止选项式API
- 组件Props、Emits必须使用defineProps/defineEmits定义完整类型
- 响应式数据优先使用ref，复杂对象使用reactive
- 组件命名使用大驼峰，模板中使用中划线分隔
- 生命周期钩子使用onXxx写法，禁止选项式API混用
- 路由、Pinia状态必须定义完整的TypeScript类型
- 禁止直接修改props，必须通过emit事件通信

---
### Python 全栈规范（适用文件：*.py）
- 严格遵循 PEP8 规范，行宽限制120字符
- 函数/类必须添加完整的类型注解，禁止使用 Any 类型
- 字符串优先使用f-string格式化，禁止%格式化、format滥用
- 异常捕获必须指定具体异常类型，禁止裸 except: 吞异常
- 优先使用pathlib处理路径，禁止硬编码路径分隔符
- 测试用例使用 pytest 框架，遵循 AAA（Arrange-Act-Assert）模式
- 导入顺序：标准库→第三方库→本地模块，禁止循环导入

---
### Go 后端规范（适用文件：*.go）
- 严格遵循 Go 官方编码规范，使用gofmt/goimports自动格式化
- 包名统一使用小写，不使用下划线、中划线，与目录名保持一致
- 错误必须显式处理，禁止使用 _ 丢弃错误，禁止忽略异常
- 命名简洁语义化，局部变量优先短命名，全局变量语义化完整
- 结构体、接口、函数必须添加规范的文档注释
- 测试用例使用原生testing框架，文件名以_test.go结尾
- 禁止goroutine泄露，必须控制生命周期、添加退出机制
- 优先使用标准库，禁止引入不必要的第三方依赖

---
### Java 后端规范（适用文件：*.java）
- 严格遵循 Alibaba Java 开发手册规范
- 类名使用大驼峰，方法名/成员变量使用小驼峰，静态常量全大写
- 必须使用SLF4J日志框架，禁止使用System.out.println
- 集合必须指定泛型，禁止使用裸类型
- 空值处理优先使用Optional，禁止空指针风险
- 事务、权限等注解必须明确属性，禁止使用默认值
- 禁止滥用继承，优先使用组合
- 异常处理必须区分受检异常与非受检异常，禁止吞异常

---
### Rust 开发规范（适用文件：*.rs）
- 严格遵循 Rust 官方编码规范，使用rustfmt自动格式化
- 优先使用所有权、借用机制管理内存，禁止滥用unsafe
- 错误处理优先使用Result类型，禁止滥用unwrap()/expect()
- 命名遵循Rust官方规范：类型大驼峰，函数/变量蛇形命名，常量全大写
- 必须添加完整的文档注释，示例代码必须可运行
- 模块组织清晰，禁止循环依赖
- 优先使用标准库，禁止引入不必要的第三方crate

---
### SQL/数据库规范（适用文件：*.sql）
- 关键字统一大写，表名、字段名小写下划线分隔
- 必须添加表注释、字段注释，说明业务含义
- 建表必须设置主键、创建时间、更新时间、逻辑删除字段
- 索引必须按需创建，禁止冗余索引、无效索引
- 多表查询必须使用别名，避免字段冲突
- 禁止使用存储过程、触发器、视图（特殊场景除外）
- DML语句必须添加WHERE条件，禁止全表更新/删除

---
### Shell/PowerShell 脚本规范（适用文件：*.sh, *.ps1）
- Shell脚本必须添加set -euo pipefail，错误自动退出
- 必须添加完整的入参校验、使用说明
- 变量必须用双引号包裹，避免空格导致的异常
- 禁止硬编码路径，优先使用相对路径/环境变量
- 执行过程必须有清晰的日志输出，关键步骤添加注释
- PowerShell脚本必须遵循官方Verb-Noun命名规范
- 必须处理跨平台兼容问题，禁止系统专属命令滥用

---
### DevOps/云原生规范（适用文件：Dockerfile, docker-compose.yml, *.yaml, *.tf）
- Dockerfile必须使用多阶段构建，最小化镜像体积
- 必须使用官方基础镜像，禁止使用未知来源的镜像
- 容器必须以非root用户运行，禁止权限滥用
- Kubernetes资源必须添加完整的资源限制、健康检查、优雅退出
- Terraform代码必须添加完整的变量定义、输出、注释
- 配置文件必须使用环境变量注入敏感信息，禁止硬编码
- YAML文件缩进统一2空格，禁止Tab与空格混用
