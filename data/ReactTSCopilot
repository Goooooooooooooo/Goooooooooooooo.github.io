# React + TypeScript 项目 Copilot 专属规则
## 生效范围
本规则仅对当前项目生效，优先级**高于全局通用规则**，对所有内联补全、Chat对话、组件开发、代码重构、测试生成100%生效。

---

## 【最高优先级·强制执行】先讨论、后编码
本条规则优先级高于本文件所有其他规则，必须严格执行：
### 必须先确认方案、禁止直接输出代码的场景
- 新页面/新模块开发、路由配置
- 通用组件/自定义Hooks封装
- 状态管理方案设计、全局状态重构
- 接口请求封装、权限逻辑开发
- 超过20行的代码重构、性能优化
- 任何需求边界不明确、技术选型未指定的场景

### 标准对齐流程
1.  需求复述：精准复述用户的核心需求与业务场景
2.  待确认项：列出技术选型、边界条件、交互逻辑、兼容要求等核心待确认点
3.  方案大纲：输出组件拆分、数据流设计、Hooks封装方案，无具体代码
4.  等待确认：必须收到用户明确的「确认方案」指令后，才可输出代码

### 可直接编码的例外场景
仅同时满足：当前文件上下文完整、技术栈明确、需求为单行/局部代码补全、修改不超过10行、不影响核心业务逻辑。

---

## 项目技术栈锁定
- 核心框架：React 18+ + TypeScript 5.x
- 构建工具：Vite
- 路由：React Router v6
- 状态管理：Zustand（可根据项目实际替换为Redux Toolkit/Jotai）
- UI框架：（可补充项目实际使用的UI库，如Ant Design/Material UI/Shadcn UI）
- 样式方案：Tailwind CSS（可替换为CSS Modules/Styled Components）
- 测试框架：Vitest + React Testing Library
- 代码规范：ESLint + Prettier

---

## 专属编码强制规范
### 组件开发规范
- 必须使用**函数式组件 + React Hooks**，禁止类组件
- 组件必须使用大驼峰 `PascalCase` 命名，文件名与组件名完全一致
- 组件Props必须使用 `interface` 定义完整类型，**禁止使用any/unknown类型**，禁止隐式类型
- 必须为Props添加中文注释，说明每个字段的含义、是否必传、默认值
- 组件默认导出，禁止匿名函数导出
- 禁止超大组件，单个组件代码行数不超过200行，超出必须拆分为子组件/自定义Hooks

### TypeScript 规范
- 开启TypeScript严格模式（strict: true），必须添加完整的类型注解
- 禁止使用类型断言 `as any`，禁止非空断言 `!` 滥用
- 枚举使用 `enum` 定义，必须添加中文注释，禁止魔法值
- 类型定义优先使用 `interface`，类型合并使用 `type`，禁止混用
- 异步函数必须定义返回值类型，Promise必须指定泛型

### React Hooks 规范
- 自定义Hooks必须以 `use` 开头，语义化命名
- `useEffect` 必须填写完整的依赖数组，禁止遗漏依赖、禁止空依赖滥用
- 禁止在循环/条件判断/嵌套函数中调用Hooks
- 状态更新必须使用不可变数据，禁止直接修改state
- 性能优化优先使用 `React.memo`/`useMemo`/`useCallback`，禁止过度优化

### 工程化规范
- 目录结构严格遵循：`src/pages`（页面）、`src/components`（通用组件）、`src/hooks`（自定义Hooks）、`src/store`（状态管理）、`src/api`（接口请求）、`src/utils`（工具函数）
- 接口请求必须使用封装的请求实例，统一处理拦截、错误、Token
- 列表渲染必须添加**稳定的唯一key**，禁止使用index作为key
- 路由必须做懒加载、权限控制，禁止硬编码路由路径
- 必须处理加载状态、空状态、错误状态、边界异常（ErrorBoundary）
- 导入排序：React内置→第三方库→本地组件/工具，禁止循环导入

### 注释与格式规范
- 注释统一使用中文，组件、Hooks、工具函数必须添加JSDoc注释，说明功能、入参、返回值、异常
- 仅在复杂逻辑、边界条件处添加单行注释，禁止冗余注释
- 缩进统一2空格，禁止Tab与空格混用
- 保存自动格式化，严格遵守ESLint + Prettier规范

---

## 分场景固定行为规则
### 组件开发场景
- 优先拆分为UI组件+业务组件，职责单一
- 通用组件必须支持Props透传、插槽、自定义样式
- 必须添加组件使用示例，说明入参、用法
- 禁止在组件内直接发起接口请求，必须封装在hooks/api中

### 页面开发场景
- 页面组件仅做路由入口、状态聚合、子组件组合，禁止大量业务逻辑
- 页面数据必须通过状态管理/自定义Hooks管理，禁止分散在组件内
- 必须实现页面标题、权限校验、SEO相关配置

### 测试生成场景
- 生成Vitest + React Testing Library 单元测试
- 测试用例覆盖正常渲染、交互逻辑、边界条件、异常场景
- 测试用例命名语义化，清晰表达测试目标
- 遵循 AAA（Arrange-Act-Assert）模式

---

## 安全与合规约束
- 禁止硬编码Token、密钥、AK/SK等敏感信息，必须使用环境变量
- 禁止直接操作DOM，优先使用React的响应式能力
- 必须防范XSS漏洞，禁止使用dangerouslySetInnerHTML，特殊场景必须做转义
- 禁止在前端存储敏感信息，必须使用安全的存储方式
- 接口请求必须做参数校验、防重放、CSRF防护
